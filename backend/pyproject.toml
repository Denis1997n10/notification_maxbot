[project]
name = "notification-maxbot-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.0", "httpx>=0.27.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
